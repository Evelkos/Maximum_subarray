#include <math.h>
#include <vector>
#include <iostream>

std::vector<int> transform_values_into_vector(int, int);
std::vector<int> find_maximum_area(std::vector<int>);
void show_vector(std::vector<int>, int, int);
